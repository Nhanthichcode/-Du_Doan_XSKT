name: Watchdog - Danh thuc Render moi ngay

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:

jobs:
  ping-server:
    runs-on: ubuntu-latest

    steps:
      - name: Gui tin hieu Ping toi Render
        env:
          MLOPS_KEY: ${{ secrets.MLOPS_SECRET_KEY }}
        run: |
          curl -X GET "https://du-doan-xskt.onrender.com/ping" \
          -H "X-Secret-Key: $MLOPS_KEY"